#config:{!! site.stringify() !!}

{!! `http://${domain}` !!} {
	proxy / {{ site.source }} {
		transparent
		websocket
		fail_timeout 0s
	}
}

# {!! `https://${domain}` !!} {
# 	proxy / {{ site.source }} {
# 		transparent
# 		websocket
# 		fail_timeout 0s
# 	}
# }
